CREATE DATABASE IF NOT EXISTS busresv;
USE busresv;

-- Bus table
CREATE TABLE bus (
  id INT PRIMARY KEY,
  is_ac TINYINT(1) NOT NULL DEFAULT 0,
  capacity INT NOT NULL
);

-- Booking table
CREATE TABLE booking (
  id INT AUTO_INCREMENT PRIMARY KEY,
  passenger_name VARCHAR(255) NOT NULL,
  bus_no INT NOT NULL,
  travel_date DATE NOT NULL,
  FOREIGN KEY (bus_no) REFERENCES bus(id)
);

-- Sample data
INSERT INTO bus (id, is_ac, capacity) VALUES
(1, 1, 3),
(2, 0, 3),
(3, 1, 3),
(4, 1, 3);
